@extends('layouts.flowerflow')
@section('title', 'Inicio')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><p class="ff-kicker mb-1">Cuenta participante</p><h1 class="mb-0">Hola, {{ auth()->user()->name }}</h1></div>@if(config('flowerflow.flags.submissions'))<a class="btn btn-flower" href="{{ route('submissions.create') }}">Nueva propuesta</a>@else<span class="badge text-bg-secondary p-2">Recepción deshabilitada</span>@endif</div>
<div class="row g-4 mb-4"><div class="col-md-4"><div class="card ff-card p-4"><span class="text-muted">Propuestas</span><strong class="display-5">{{ $submissions->count() }}</strong><small>Máximo 3</small></div></div><div class="col-md-4"><div class="card ff-card p-4"><span class="text-muted">Enviadas</span><strong class="display-5">{{ $submissions->where('status', 'submitted')->count() }}</strong><small>Con folio</small></div></div><div class="col-md-4"><div class="card ff-card p-4"><span class="text-muted">Perfil</span><strong class="h3 mt-2">{{ auth()->user()->profile?->isComplete() ? 'Completo' : 'Pendiente' }}</strong><a href="{{ route('profile.edit') }}">Revisar perfil</a></div></div></div>
<div class="card ff-card"><div class="card-body"><div class="d-flex justify-content-between"><h2 class="h4">Mis propuestas recientes</h2><a href="{{ route('submissions.index') }}">Ver todas</a></div>@if($submissions->isEmpty())<p class="text-muted mb-0">Aún no tienes propuestas.</p>@else<div class="table-responsive"><table class="table"><thead><tr><th>Título</th><th>Categoría</th><th>Estado</th><th></th></tr></thead><tbody>@foreach($submissions as $item)<tr><td>{{ $item->title }}</td><td>{{ $item->category->name }}</td><td><span class="badge {{ $item->status === 'submitted' ? 'badge-submitted' : 'badge-draft' }}">{{ $item->statusLabel() }}</span></td><td><a href="{{ route('submissions.show', $item) }}">Abrir</a></td></tr>@endforeach</tbody></table></div>@endif</div></div>
@endsection
