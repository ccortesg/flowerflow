@extends('layouts.flowerflow')
@section('title', ($panel ?? false) ? 'Acceso al panel' : 'Iniciar sesión')
@section('content')
<section class="ff-section"><div class="container"><div class="row justify-content-center"><div class="col-md-7 col-lg-5"><div class="card ff-card p-4"><div class="card-body"><p class="ff-kicker">{{ ($panel ?? false) ? 'Administración' : 'Cuenta participante' }}</p><h1 class="h2">{{ ($panel ?? false) ? 'Acceso al panel' : 'Iniciar sesión' }}</h1><form method="POST" action="{{ route('login') }}" class="mt-4">@csrf<div class="mb-3"><label class="form-label" for="email">Correo electrónico</label><input class="form-control" id="email" name="email" type="email" value="{{ old('email') }}" required autocomplete="email" autofocus></div><div class="mb-3"><label class="form-label" for="password">Contraseña</label><input class="form-control" id="password" name="password" type="password" required autocomplete="current-password"></div><div class="form-check mb-3"><input class="form-check-input" id="remember" name="remember" type="checkbox"><label class="form-check-label" for="remember">Mantener sesión</label></div><button class="btn btn-flower w-100" type="submit">Entrar</button></form><p class="mt-3 mb-0"><a href="{{ route('password.request') }}">Olvidé mi contraseña</a></p>@if(config('flowerflow.flags.registration'))<p class="mt-2 mb-0">¿Aún no tienes cuenta? <a href="{{ route('register') }}">Regístrate</a></p>@endif</div></div></div></div></div></section>
@endsection
