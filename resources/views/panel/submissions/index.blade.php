@extends('layouts.flowerflow')
@section('title', 'Propuestas del panel')
@section('content')
<p class="ff-kicker mb-1">Recepción</p><h1>Propuestas</h1><form method="GET" class="card ff-card p-3 my-4"><div class="row g-3 align-items-end"><div class="col-md-4"><label class="form-label" for="status">Estado</label><select class="form-select" id="status" name="status"><option value="">Todos</option><option value="draft" @selected(request('status') === 'draft')>Borrador</option><option value="submitted" @selected(request('status') === 'submitted')>Enviada</option></select></div><div class="col-md-5"><label class="form-label" for="category">Categoría</label><select class="form-select" id="category" name="category"><option value="">Todas</option>@foreach($categories as $category)<option value="{{ $category->slug }}" @selected(request('category') === $category->slug)>{{ $category->name }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-flower w-100">Filtrar</button></div></div></form><div class="card ff-card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Folio</th><th>Proyecto</th><th>Participante</th><th>Categoría</th><th>Estado</th><th>Fecha</th></tr></thead><tbody>@forelse($submissions as $item)<tr><td>{{ $item->folio ?: '—' }}</td><td><a href="{{ route('panel.submissions.show', $item) }}">{{ $item->title }}</a></td><td>{{ $item->user->name }}</td><td>{{ $item->category->name }}</td><td>{{ $item->statusLabel() }}</td><td>{{ $item->created_at->timezone(config('flowerflow.timezone'))->format('d/m/Y H:i') }}</td></tr>@empty<tr><td colspan="6" class="p-4">No hay resultados.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $submissions->links() }}</div>
@endsection
