@extends('layouts.flowerflow')
@section('title', $submission->title)
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-start gap-3 mb-4"><div><p class="ff-kicker mb-1">{{ $submission->category->name }}</p><h1>{{ $submission->title }}</h1><p><span class="badge {{ $submission->status === 'submitted' ? 'badge-submitted' : 'badge-draft' }}">{{ $submission->statusLabel() }}</span>@if($submission->folio) <strong class="ms-2">Folio {{ $submission->folio }}</strong>@endif</p></div>@if($submission->isDraft() && config('flowerflow.flags.submissions'))<a class="btn btn-outline-dark" href="{{ route('submissions.edit', $submission) }}">Editar borrador</a>@endif</div>
<div class="row g-4"><div class="col-lg-8"><article class="card ff-card p-4"><h2 class="h4">Resumen</h2><p>{{ $submission->summary }}</p><h2 class="h4 mt-3">Descripción</h2><div class="prose">{!! $safeHtml !!}</div></article></div><div class="col-lg-4"><aside class="card ff-card p-4"><h2 class="h4">Detalles</h2><dl><dt>Modalidad</dt><dd>{{ $submission->participation_type === 'team' ? 'Equipo' : 'Individual' }}</dd><dt>Archivos</dt><dd>{{ $submission->files->count() }}</dd>@if($submission->submitted_at)<dt>Enviada</dt><dd>{{ $submission->submitted_at->timezone(config('flowerflow.timezone'))->format('d/m/Y H:i') }} (Hermosillo)</dd>@endif</dl><ul class="list-unstyled">@foreach($submission->files as $file)<li class="mb-2"><a href="{{ route('submissions.files.download', [$submission, $file]) }}">{{ $file->original_name }}</a></li>@endforeach</ul></aside></div></div>
@if($submission->isDraft() && config('flowerflow.flags.submissions'))<form method="POST" action="{{ route('submissions.submit', $submission) }}" class="card ff-card p-4 mt-4" data-confirm="Al enviar se congelará una versión y ya no podrás editarla. ¿Continuar?">@csrf<h2 class="h3">4. Revisar y enviar</h2><p>Esta acción genera el folio y el snapshot inmutable.</p><div class="form-check mb-3"><input class="form-check-input" id="accept_call_rules" name="accept_call_rules" type="checkbox" value="1" required><label class="form-check-label" for="accept_call_rules">He leído y acepto la Mecánica de la Convocatoria.</label></div><div class="form-check mb-3"><input class="form-check-input" id="accept_terms" name="accept_terms" type="checkbox" value="1" required><label class="form-check-label" for="accept_terms">He leído y acepto los Términos y Condiciones.</label></div><div class="form-check mb-3"><input class="form-check-input" id="accept_privacy" name="accept_privacy" type="checkbox" value="1" required><label class="form-check-label" for="accept_privacy">He leído y acepto el Aviso de Privacidad Integral.</label></div><div class="form-check mb-4"><input type="hidden" name="optional_updates" value="0"><input class="form-check-input" id="optional_updates" name="optional_updates" type="checkbox" value="1"><label class="form-check-label" for="optional_updates">Deseo recibir avisos opcionales sobre actividades relacionadas. Puedo retirar esta autorización.</label></div><button class="btn btn-flower btn-lg align-self-start">Enviar propuesta</button></form>@endif
@endsection
